# 必须遵守规则！！！
1. 不确定时要询问，不要默默选择一种解决方式就直接开始
2. 代码最简化，严禁过度设计
3. 只修改必要的部分，不要随便修改旁边的代码
4. 在开始前将模糊的指令转化为可验证的目标

## 技术栈
- 前端：vue3
- 后端：go
- 数据库：mysql(3306端口，密码422624)
- agent：langchain/py（key：）
- 模型调用 qwen的vl只做视觉理解理解的结果文字输出，然后转接max做处理，max做输出等，api：sk-45fb82ec310b47b4842e9bd161b8dd5c
qwen千问官方的api

## 功能描述
#### 做一个ai智能体，可以根据用户的需求，给出不同的格式化回答
- 用户给出食物图片或者食物名称等描述，ai智能体可以识别出食物并给出相应的热量、蛋白质、脂肪、碳水化合物含量信息
- 用户给出减肥健身等需求和个人信息，ai智能体可以给出相应的运动计划和饮食建议

## 项目结构

### agent_py

#### Pipeline 强制结构（强制线性，禁止agent流程）：

RunnableSequence([
    step1_llm,
    step2_tool,
    step3_llm,
    step4_validator
])

禁止：
- if/else
- for/while
- try/except 控制流程
- AgentExecutor

#### 工具函数（tools）职责：

- 只负责：
  - API调用
  - 数据清洗
- 严禁：
  - 调用 LLM
  - 写 prompt

#### Agent实现规范
- 使用 LangChain Expression Language (LCEL)
- 不使用 AgentExecutor
- 严禁自定义复杂 control flow
- 严禁多分支逻辑（if/else）
- 必须线性 pipeline
- 使用 RunnableSequence

每个1_,2_，modeselection这样的文件夹代表一个功能模块,都分设两个文件，一个main用于写主流程，一个tools用于写工具函数（为了让主流程代码更清晰，所以才分的文件）

#### 前端对模式的选择，直接进行进入pipline流程，这些pipline流程并非agent工具，不是给llm调用的，而是直接的运行流程，严禁llm参与调用延长运行时间
#### 对于1_食物识别，使用pipline做流程：
1. 用户输入食物图片（如果是图片，将用户输入的食物图片或者食物名称发送给llm，让llm分析用户输入的食物并返回食物名称 json格式：{"food_name": "string"}，然后用这个食物名再做后续流程）或者食物名称
2. 调用tools中的工具函数：设计工具用api调用usda数据，经过处理之后发给llm，让llm分析食物的热量、蛋白质、脂肪、碳水化合物含量信息，查看是否符合常理（usda有可能返回了错误的对象，工具示例放在了旁边的usdaapi.py文件中,包括测试返回结果，你需要按照结果再洗去无用数据再发给llm），如果返回正确，正常返回；否则，直接由llm模型进行评估可能的热量等信息，给用户模型评估的数据。
3. 返回固定格式json结果（食物名称、热量、蛋白质、脂肪、碳水化合物（都是每100g含量）），不要多任何东西
{
  "type": "object",
  "required": [
    "food_name",
    "calories_per_100g",
    "protein_per_100g",
    "fat_per_100g",
    "carbs_per_100g"
  ],
  "properties": {
    "food_name": { "type": "string" },
    "calories_per_100g": { "type": "number" },
    "protein_per_100g": { "type": "number" },
    "fat_per_100g": { "type": "number" },
    "carbs_per_100g": { "type": "number" }
  }
}/无法识别时返回：
{
  "error": "FOOD_NOT_FOUND"
}

#### 对于2_运动计划，使用pipline做流程：
1. 用户输入问题，携带用户信息给ai（后端从数据库获取用户信息并传递给ai）
2. 调用tools中的工具函数：将用户问题和提示词发送给llm，让llm分析用户需求并返回运动计划
3. 返回固定格式json结果（运动计划：默认三个月的计划，每个月只定义周几分别做什么运动，还有一个月的总目标和总饮食建议，记得要每周有休息日），不要多任何东西
{
  "duration": "3_months",
  "total_goal": "string",
  "months": [
    {
      "month": 1,
      "goal": "string",
      "diet_advice": "string",
      "weekly_plan": {
        "monday": [
          {
            "activity": "push_up",
            "sets": 2,
            "reps": 8,
            "duration_min": null,
            "intensity": "medium"
          },
          {
            "activity": "running",
            "sets": 1,
            "reps": null,
            "duration_min": 20,
            "intensity": "low"
          }
        ],
        "tuesday": [],
        "wednesday": [],
        "thursday": [],
        "friday": [],
        "saturday": [],
        "sunday": [
          {
            "activity": "rest"
          }
        ]
      }
    }
  ]
}
4. 返回结果后，前端要进行渲染，渲染成表格形式，并且要自动更新到用户的mysql数据里，让用户能够自行查看
#### 对于3_changeplan，使用pipline做流程：
1. 用户输入问题，携带用户信息给ai（后端从数据库获取用户信息并传递给ai）
2. 调用tools中的工具函数：将用户问题和提示词发送给llm，让llm分析用户需求并返回计划修改，修改要只针对用户要求，不要做大更改或者重新制定计划
3. 返回固定格式json结果（格式和运动计划一样），不要多任何东西
{
  "modified": true,
  "changes": [
    "替换周三跑步为游泳"
  ],
  "new_plan": { ...完整结构同2_make_plan(你写提示词的时候要完整写) }
}

#### ## Prompt管理

每个模块必须有：
- prompts/
    - system.txt：核心：You are a strict JSON generator.

If you output anything other than JSON:
- Your response will be rejected
- You must regenerate

Do not explain.
Do not add text.
Only output JSON.
    - user.txt
禁止在代码中写长prompt
{{user_input}}
{{user_profile}}
{{existing_plan}}

#### 对于4_其他功能，使用pipline做流程：
1. 用户输入问题
2. 调用tools中的工具函数：将用户问题和提示词发送给llm，让llm分析用户需求并返回其他功能
3. 返回llm返回的内容

#### 加“输出校验层”
(如果JSON格式不正确，就重新生成）

#### 本项目不使用传统Agent（ReAct）
所有流程：
- LLM仅负责生成结果
- 不参与决策流程


### backend_go
1. 连接mysql数据库
2. 登陆注册
3. 用户信息（包括用户的基本信息：身高、体重、bmi（前端自动计算，不用储存）、年龄、性别、核心诉求（有标签供用户选择（产后恢复，减脂，增肌等，你自己做调研看看哪些频率高））、喜欢的运动、是否将运动加入训练计划、训运动的描述（一周几次，大概时长，强度）、期望训练强度（新手，正常，硬汉）、运动计划等，其中身高、体重、年龄、性别、核心诉求为必填项，用户登录直接弹弹窗让用户填写）
4. 接口分类明确，按照功能分类进文件夹（AgentQuestion、PersonInfo、SQLinit），再按更详细的相关性分进各个go文件，文件名都用易懂的中文显示
5. 对ai询问时，对话框上面含有选择模式：1_食物识别，2_制定计划，3_修改计划，默认是其他功能
6. backend_go 通过 HTTP 调用 agent
7. 做一个通过时间解析今日计划的功能，让用户能够知道今天具体要做的任务，任务后面有完成按钮，上面有完成几分之几的进度条

### front_vue
npx getdesign@latest add mistral.ai
用上面的指令获取mistral.ai的ui组件库，然后用这个组件库来构建前端页面，风格相同，我已经下载完成，你直接查看design.md

我下载了chatgpt_web的源码，你进行参考，对于前端，我想要打造一个类似chatgpt的ai智能体对话web，但是风格需要是mistral.ai的风格，详细要求：
1. 你要做单独的注册和登录界面
2. 主页功能性菜单等都在左侧，上面是菜单区域：默认进入是今日计划功能，其次就是最后主要的ai对话功能，还有查看我的计划功能和查看身体数据功能，菜单栏下面放与ai对话的目录，菜单栏最下面放置用户按钮，点击后弹出用户信息界面（退出登录，改名等）
3. 对于ai对话的目录，需要有历史对话记录，可以点击进入历史对话记录，也可以新建对话
4. 对于与ai对话的输入栏：要有图片插入功能（可以点击添加图片，也可以拖拽，复制粘贴上传），上面有模式选择功能，可以选择模式1_食物识别，2_制定计划，3_修改计划，默认是其他功能
5. 对于ai对话的输出，要进行渲染表格化展示，具体渲染格式参考返回的json具体格式
6. ui颜色风格等不要按照chatgpt_web，而是参考mistral.ai的ui风格，并对于元素都加上适当圆角和亚克力透明效果显得更加现代化


#### 前端约束
- 使用 Vue3 + setup script
- 所有接口统一走 /api 前缀
- axios 封装 request

## 全局要求：
严禁多写非文档中的功能
严禁偏离文档要求，如需更改，需要进行询问
严禁更改文档内容
如果需要修改的文件多，先检索文件，然后将每个文件都放在任务栏中，防止漏文件


### 补充：
使用fastapi，用gin框架，模型服务商：阿里云 DashScope

模型：

1. 多模态：
   model = "qwen-vl-plus"

2. 推理模型：
   model = "qwen-max"，允许联网运行，host: localhost
port: 3306
user: root
password: 422624
database: ai_agent
charset: utf8mb4

3. 外置bat文件一键启动

4. key等敏感信息放在放在环境文件中，通过提取使用

5. 与ai对话的模式选择的按键放置在输入栏的上方，将模式平铺在输入框上面，点击一个那个就会亮起，上一个点击的就会熄灭，默认放在其他上面，几个按钮分别叫做：食物分析、制定计划、修改计划、其他

6. 对自己运动的描述仅仅在打开将运动加入训练计划时才会出现。

7. 核心诉求你需要多做具体调研，多列举一些标签来供用户选择，且可以多选

8. 年龄的设置是设置出生日期，而不是输入多少岁了，再增加两个标签：详细诉求和伤病史

9. 对于出生日期，身高，体重，性别，训练强度这些重要内容，是必填项，还有核心诉求多一个其他标签，核心诉求是必填的，如果选择其他标签，那么用户必填详细诉求，否则用户点击ai对话界面，就会弹窗告诉用户完善个人信息，用一个跳转让用户去填写（必填）

10. 点击左下角用户头像，要直接跳出来弹窗格式的个人中心，应该包含名称、头像以及他们的修改，还有重设密码，退出登录，不要显示个人身体数据，这是单独的。对于头像，resize图片到150*150格式，然后用二进制存到mysql数据库


11. 增加对话的删除功能，点击对话记录的删除按钮，会弹出确认删除的提示，确认后删除该对话记录

12. ai对话功能中如果复制进来图片，那么图片预览显示应该放在模式选择之上，防止遮挡模式选择

13. 与ai的对话应该按时间显示，选择对话等进入对话之后，直接显示该对话的最低部最新消息，更符合人性化

14. 如果用户是新建用户没有历史对话，或者默认情况下或者历史对话都删除了的情况下，直接给ai发信息等，要自动新建对话，还有，新建对话的时候如果用户没有发信息，就用时间来标题，如果用户发送了第一条请求，就用第一条请求的内容（仅文字生效）来重新命名对话

15. 将图片拖入对话的时候，应该显示请将图片拖入此处的框，而且要适当大一些，让用户方便操作

16. 后端默认使用详细日志的debug模式，方便调试，还有pipline进行到各步骤，都要在后端进行详细结果输出，同时，前端的省略号表示的思考过程也要详细展示到哪一个步骤了（即后端pipline流程的简短名称）让用户等待不急躁，知道模型在运行

17. 后端显示的日志要详细，对于每个简易步骤（是的，每一步都需要）都要加检查，如果出现错误，能够明确定义小错误点和大错误点

18. 对ai对话的图片添加压缩功能，如果短边大于224px，那么就压缩到224px，长边按比例压缩，else，长边大于500px，压缩到500px，短边按比例压缩

19. 对话记录也按照用户id和对话id储存到数据库，图片也用二进制储存到mysql（压缩按照18条规则），前端请求聊天记录的时候刚进去最多请求五来回对话，每次下拉在顶部中心做一个转圈的请求等待，然后每次请求三来回记录

20. 与ai的对话，用户发送的头像应该显示为用户自己设定的头像，没设置则用默认头像

21. 对于食物识别，如果图片识别模型未识别出食物，那么将再进行最多一次的重新识别，如果还是没有成功识别，则返回用户：图片中未找到食物，请拍摄清楚或者靠近拍摄

22. 个人身体数据编辑更新之后，身体数据界面需要自动做一次刷新，显示最新的信息

23. 对于体重，使用单位改为可选：用户可在个人身体数据编辑界面改变：斤、KG，同时BMI计算等也需要更改

24. 对于体重更新，对于体重后来的更新方式，放在身体数据里体重卡片的右下位置和今日计划的偏底部位置，点击后弹出更新体重和体重统计表等：对于显示的体重是由7天内体重数据不同权重构成的，越新的数据权重越大，权重按照对数算法计算（你先计算出权重，作为固定权重，后面直接乘即可，一天如果更新多次数据，则做平均当天处理），还要增加筛选（比7天内数据中位数大于或小于3kg的数据直接做抛弃处理），且用户在填写新体重数据的时候需要选择空腹、正常、饱腹的状态，而对于计算的时候：体重以空腹为标准，对于用户输入的饱腹和正常状态的数据，要在这个数据上减去一个数值，默认情况下是体重（float）除以50kg（注意单位换算）\*数值，这个数值对于男性，空腹到正常设置为0.6kg、正常到饱腹设置为0.3kg，对于女性，空腹到正常设置为0.5kg，正常到饱腹设置为0.2kg，而这个数值也会动态变化：如果采集到用户输入饱腹或者正常的数据，按照原数据\*0.9+（输入数据-现在计算的平均空腹体重）\*0.1进行更新做为用户自己的参数（如果输入饱腹的数值则先按照空腹到正常的参数计算出预测的现在正常的平均体重，再在这个基础上更新正常到饱腹的参数）。对于体重数据和参数都存进数据库，再做一个体重变化折线图（分为单点的具体数据（空腹）（即上面模型计算的空腹体重）和平滑曲线表示趋势），然后对于显示数据，后台计算都使用kg为准，对于最后的结果用四舍五入保留一位小数显示，如果用户选择斤做单位，则在计算后直接简单乘以2，表格展示显示为长条形状，能够左右滑动查看，能够选择日、三日、周，然后按月份打点，鼠标瞄准的那一竖列需要显示虚线并详细显示数据，其他点数据不做展示，仅仅依靠左侧y轴表示，y轴也要有动态变化：以当前显示的数据最大值和最小值为准，上下留的空白空间至少为10%，且Y轴总跨度不少于10kg，对于最新的体重数据，显示一条贯穿整图的虚线供用户参考

25. 今日计划界面，偏下面部分放置一个更新体重的按钮，方便用户在不同界面更新体重，而对于个人数据的更新体重，应该使用主页使用的的橙色做底，白色字体，显得美观，更新体重界面，输入体重，后面不用选择单位，用户在编辑信息界面设置的斤还是kg是作为用户的信息存入数据库，网站上所有的重量显示都应该按照用户选择的来显示，空腹等状态选择将三个标签平铺，用点击点亮的方式进行选择，默认是空腹状态，点击后点亮，其他两个标签自动熄灭。

26. 在个人数据界面，保留更新体重按钮的同时，点击体重整个卡片都会进入更新体重界面，而对于编辑资料里面的体重不用写无法修改，将数值正常显示，而且点击的时候进入更新体重界面，

27. 对于编辑资料界面的kg和斤的选择，也应该贴合主题的ui设计，做成圆角等

28. 对于制定计划，回答的格式应该按照我所写的，每天都需要具体的每一项activity，每天有多少activity不做限制，不过每个都要写具体，对于前端渲染的计划表格：每周几竖着排列，右边分别是activity和几组几次、这一天的强度、大概需要时间，做成这样的表格，对话和查看我的计划都这样渲染

29. 对于计划制定，需要详细制定每一天的计划，而不是笼统概括或者简单描述，比如用户说“帮我制定一个增肌计划”，应该详细列出每一天的训练内容，而不是只说“周一练胸、周二练背”这样简单的概括，比如如果用户想要增肌，就我给你一个一天的训练计划的例子：弯举10\*4、卧推10\*4、深蹲15\*4、平板支撑40秒\*4组这样具体列举出每天的各个计划都应该是什么（对于动作，你应该根据用户的身体数据和目标训练肌群来选择合适的动作，而不是随意选择），还要考虑到每周每个需要训练的肌群都要训练到，根据这样的我要的效果来重新写提示词等，并自行运行，重复调试出能有合适的效果，而对于跑步等其他活动，也要说清楚什么强度，比如用配速或者心率区间来描述强度

30. 对于次数\*组次，因为可能含有跑步等，所以可能不是单纯的次数\*组次，比如跑步可能是5公里\*3组，或者时间*组数，所以应该改为str格式，并对ai做出提示，让他写清楚

31. 对于制定计划中表格中的强度，应该一天只写一个强度，用来描述这一天的整体强度，而不是每个activity都写一个强度，对于这个，你将计划书中的json严格格式也进行优化，然后提示词进行同步优化

32. 对于制定计划中的时间，前端应该渲染出预计需要时间，然后给ai的提示词也应该用来描述为计算这一天总训练预计需要多长时间

33. 对于制定计划，多加一个标签：组间间歇时间，用来描述每组之间的休息时间，如果此天不用分组，则此标签空白


34. 编辑资料里面的体重不能够直接更改的，而应该是点击体重那一栏，直接显示更新体重的那个界面进行算法更新

35. 更新体重界面应该添加一个按钮，点击能够显示历史体重记录，包括日期和体重以及空腹等状态，可进行删除操作

36. 制定训练计划，优化提示词：如果某天是纯力量训练，那么对于力量训练，动作不应该只设置一个，至少应该有四个动作（根据用户选择的强度递增），而且要涵盖不同的肌群

37. 对于制定计划，指定的时间不严格按照三个月，要根据用户的输入进行调整，如果用户没有描述，则按照三个月，且优化提示词：训练计划如果三个月不是指只练三个月，所以不要以为第一个月就该放松，第三个月就该冲刺，训练要人性化，渐进，且贴合用户具体需求

38. 对于制定计划，强度栏的描述不应该加配速心率等信息（仅仅只需要简单的强度描述即可（高低强度休息等）），这些信息应该直接放在activity里面作为对每个项目的具体描述，而对于跑步等：可以用配速描述，也可以用心率区间来描述，不过力量训练用心率区间则没有必要

39. 对于制定计划，如果某天是力量训练，那么对于力量训练，动作不应该只设置一个，至少应该有四个动作（根据用户选择的强度递增），而且要涵盖不同的肌群，对于深蹲和硬拉这些都算是力量训练，那么他们如果单独存在那一天的训练量也有点太少了，只有简单的一个动作做组，非常的不合理

40. 每个月计划的本月目标是固定的吗？本月目标：冲刺阶段，加强速度与耐力结合，这是现在我测试的每次第三个月都这样，不要让他固定，应该用准确些的提示词来描述目标，比如如果用户想要减肥，那么目标可以围绕减少到多少斤/KG来描述，如果用户想要跑步等，可以使用公里配速来描述，当然也不要用这样一直用一种数据做每个月的目标，可以灵活变化，这里仅作举例，也就是说目标应该根据用户的具体需求来描述，而不是固定的。

41. 还有我的详细诉求里说八个月后的马拉松比赛，但是返回的第三个月都让我准备冲刺，包括目标和饮食等，还让我模拟比赛，我现在想要的设计是想让ai按照用户的需求来生成几个月的训练计划，默认是三个月，但是比如用户8个月备赛，只生成3个月，也不应该第三个月就该冲刺了

42. 对于体重记录的那个图表显示，现在只按照输入的日期来显示，但是这样看在时间轴上不好，那么比如我某一天没有更新体重，没有哪个记录，那么你应该在时间轴上显示这个日期，不过上面不应该有数据，是空的，不要用点描述那个日期，然后折线图就从前面有数据的直接连接后面有数据的，而对于平滑数据曲线，你进行修改算法，因为他需要跨越没有数据的点，而且没有数据的点不应该有数据提供给平滑算法，他需要在中间没有数据的情况下保持平滑

43. 对于体重记录的那个图表显示，对于鼠标悬浮在折线上的点显示具体数据，不应该只是精确悬浮到点上才显示信息，那样体验不好，应该鼠标在点所在的时间范围内：前0.5天和后0.5天在折现统计图上，他都会显示对应日期的数据

44. 对于体重记录的那个图表显示，在时间轴上，应该至少有10个日期，不要如果有三个点，那么第一个在开头，最后一个就到结尾了，但实际才三个数据，然后拉了很长很直的曲线，应该至少有10个横轴点，从用户开始有数据的一个点开始做为初始，然后后面即使是未来的时间，那么就显示上日期，没有数据即可，对逻辑进行重构

45. 现在点击ai对话界面就会自动新建一个对话，但是用户有时候会只是点击一下，然后就自动新建对话列表了，然后用户不用就空白了，这样不合适，所以改为只有用户在非历史聊天发送信息再新建对话，这样就不会有一堆空的对话列表了

46. 对于计划制定，增加一个曾经计划的选项，用户可以查看曾经制定的计划（最多储存两个历史），并且可以对历史计划进行删除和替换为当前计划（将这个数据提取到当前计划，然后将当前计划变为最新的历史计划），修改计划功能对计划的更改也会让原计划变成历史计划，然后用修改后的计划变为新计划

47. 对于制定计划对话，增加一个检测，如果用户当前有计划，发送制定计划功能消息时，会显示有弹窗提示，提示用户是否要替换当前计划，如果选择替换，就继续发送信息，如果用户点击取消，不发送信息了，用户的信息依旧还在输入框里，等待用户再次发送，再加一个倒计时，这个弹窗显示10秒倒计时（按秒变化数字），倒计时结束将自动选择替换而发送消息（将倒计时加到替换的按钮旁边，让用户明白倒计时结束时是自动选择替换）


48. 对于其他问题模式的回答，需要用提示词等将模型打造成为一个专业的顾问，而不是一个简单的ai回复助手，比如现在没有任何提示词的ai，给我的回答是：{"answer":"做卷腹或仰卧起坐时感到尾椎骨不适，可能是因为你的动作不够标准或是垫子太薄。建议使用较厚的瑜伽垫，并确保动作正确，避免直接压迫到尾椎骨。如果问题持续存在，最好咨询专业的健身教练或医生。"}，首先，需要对这个回答进行前端渲染，而且这样的回答太简略，他说确保动作正确，但是没有说明如何确保，应该详细说明动作标准和注意事项，还有咨询专业的健身教练和医生，但是我打造的是专业的aiagent助手，这样显得不专业，需要避免这些词的出现，而应该是详细的解释和建议

49. 对于修改计划模式的回答，应该返回具体的更改描述，而且前端应该渲染出来作为回答让用户看到

50. 新增memory功能，每当用户和aiagent对话后，再新建一个agent_py的文件夹，和其他的一样，做一个pipline流程：每次用户在除了图片热量识别模式对话之后，将用户发送的信息和已有的memory发送给ai（也使用max模型），然后用提示词告诉ai：如果对于对用户的个性化健康助手来讲，用户这次发送的信息里面是否包含需要储存的用户的身体信息（比如用户做不了什么动作之类的），然后将这些信息简略提取出来，同时要考虑是否已经有该记忆被存过了就不需要再次储存，还有如果有冲突的（及其矛盾的）记忆，应该优先保留最新的记忆，然后将记忆通过固定格式的json发回后端（包含原来的，原来的不冲突则不需要修改），一起总结，然后后端再次进行重新储存，然后还需要对原有流程进行更新，除了图片热量识别模式对话之外，其他模式对话都需要在发送给ai信息的时候带上memory（和其他个人信息一起），然后memory是需要分点的，用户可以在个人信息界面编辑资料里面弄一个按钮，点击可以查看现有记忆，可以删除（不允许修改），数据库也需要重新加标签

51. memory功能启动是在ai正常回答完用户问题之后的，是在后台请求的，不能让memory拖慢用户正常的交流所需时间

52. 对于食物识别功能，第一个模型返回的时候，也需要判断中文名字，因为用户全部是中文使用者，所以正常返回的英文用来调用api查询数据库，而中文名字用来最后输出给用户让用户知道是什么

53. 前端进行优化，手机端展示是需要的，你进行阅读前面对于前端的要求，将所有的功能都适配手机等小屏幕版本（仅对小屏做元素隐藏等，现在的大屏模式不要做改变），风格保持一致，现在的电脑端ui保持不变，并添加昼夜变换模式，现在的暗基调作为夜间模式（注意现在的配色很好，不用改，就用黑+橙作为夜间模式，白+橙作为日间模式），日间黑白配色互换，这些前端都可以再次gpt_web
项目，注意别把现在的完善大屏夜间UI系统给改坏了

54. 对于手机等小屏设备的UI优化：查看身体数据界面，整个界面长度太长了，但其实没有多少信息，将身高体重等卡片做成正方形，bmi还是长方形，不过竖向缩短点，让他们能够一个屏幕尽量排开（对于900左右的px手机）；今日计划界面，手机版的进度条消失了，还有整体强度那个卡片竖向也适当压缩一些些；我的训练计划中历史计划固定占据了上方很大一部分，将手机版的改为按钮显示；对于手机版发送图片，图片信息占据了横向全部像素，显得不美观，适当缩小一下；对于日间模式对食物热量表格的返回，现在显示的表格还是黑底的，也应该随着日间模式变为白色；最上方的ai健康助手，改为每个模式显示每个模式的名称，今日计划模式就写今日计划；手机版的ai对话模式，现在上方板块显示的ai对话和AI已就绪占空间，对于手机版直接隐藏，大屏版不用；对于大屏版，现在选择模式，鼠标悬浮在模式上面，字直接消失了，变得和背景一个颜色了，这样看不见，不好，改掉

55. 设计会员制功能，88元会员费，每个新用户可以免费体验一个月，会员界面从左下角头像处点开查看，购买界面做三个卡片，第一个FERR：仅可使用食物识别和其他两个模式的ai对话以及基础功能，核心的制定计划和修改计划、今日计划功能不能使用，memory功能不能使用；TRy可以对每个用户免费试用一个月的PRO计划；而Pro计划分为半年卡48元和年卡88元，会员权益解锁所有功能，要简略列举功能，商城价格95折扣，这四个套餐做成四个卡片，点击然后下面空间做此套餐的描述，且一年按照365天计算，是向下取整52周，每周完成全勤是指当周的训练计划除了rest的日子，用户只要登录即可算当日签到了，然后一周全勤的话算签到一周，对于年制会员，签到10周则返还10%会员费，签到30周返还30%会员费、签到48周返还50%会员费，半年制则周数减半，其中返还的会员费直接返还到用户余额中，可以进行提现、购买商城商品、续费下次会员

56. 制作商城功能，商城功能做到左侧菜单栏里面，和其他ai对话等功能并列，商城做分类商品，你自己调研健身类以及其他商城都有什么商品类别，然后你先不用寻找图片等，给我每个分类做成文件夹，然后我放进去图片以及命名格式为‘名称_价格.jpg’你进行渲染你告诉我文件夹都在哪，只在前端按照类别名称命名即可，现在先做这样一个假的示范，我还没拉到合作