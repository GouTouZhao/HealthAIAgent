# AI 智能健身助手项目报告

## 一、项目定位与目标

本项目是一个面向个人健康管理场景的 **AI 智能体应用**。它不是单一聊天工具，而是把“问答、识别、计划、执行、追踪、记忆”串成闭环的健康助手系统。

项目核心目标：
- 让用户通过自然对话获得可执行的健康建议，而不是泛泛回复。
- 将训练计划从“周一练胸”这类粗粒度建议，升级为“每天具体动作+组次+时长+强度”的结构化方案。
- 打通“计划制定 → 今日执行 → 体重追踪 → 计划调整 → 记忆沉淀”的连续链路。
- 以长期个性化为导向，让系统逐步理解用户约束（伤病、禁忌动作、偏好、目标等）。

---

## 二、技术栈（简要）

- 前端：`Vue 3` + `Vite` + `Pinia` + `Vue Router` + `Axios`
- 后端：`Go` + `Gin` + `GORM`
- 数据库：`MySQL`
- AI 服务：`FastAPI` + `LangChain LCEL`
- 模型：阿里云 DashScope（`qwen-vl-plus`、`qwen-max`）
- 运行方式：根目录 `start.bat` 一键启动前端/后端/AI 服务

---

## 三、核心技术能力（简要）

- **AI Agent 功能链统一化**：各 AI 能力都采用统一的 4 步结构（输入整理、上下文构建、模型生成、结构校验），输出稳定。
- **严格 JSON 约束与自动纠偏**：模型输出必须为结构化 JSON，失败会自动重试修正，保障前端可渲染。
- **流式过程可视化**：前端可看到 AI 当前执行步骤，降低等待焦虑。
- **强个性化上下文注入**：用户画像、当前计划、历史记忆会参与 AI 生成过程。
- **体重智能计算与可视化**：支持状态换算、异常过滤、时间加权、趋势曲线展示。

---

## 四、功能全景（逐项详细说明）

## 4.1 账号与身份功能

### 4.1.1 注册与登录
系统提供独立注册/登录入口，用户完成认证后进入主工作区，具备基础会话状态管理能力。

### 4.1.2 个人中心
用户可在主界面左下角打开个人中心，进行：
- 用户名更新
- 头像上传与更新（统一压缩为 `150x150`）
- 密码重置
- 退出登录

个人中心聚焦“账号管理”，与“身体数据管理”解耦，降低操作复杂度。

---

## 4.2 用户健康画像功能

### 4.2.1 完整档案建模
系统建立了完整健康画像，包括身高、体重、出生日期、性别、训练强度、核心诉求、详细诉求、伤病史、运动偏好等信息。

### 4.2.2 必填与质量约束
对关键字段设置必填校验；当用户信息不完整时，进入 AI 对话会触发完善提示并引导补全，保证后续建议的可靠性。

### 4.2.3 核心诉求多标签
支持多选核心目标（如减脂、增肌、体能提升、马拉松备战等），并支持“其他”分支下的详细诉求补充，增强目标表达能力。

### 4.2.4 体重单位与 BMI 联动
支持 `kg/斤` 双单位，展示与计算同步联动，BMI 与状态解释（偏瘦/正常/超重/肥胖）即时更新。

### 4.2.5 运动描述条件展示
仅当用户开启“将喜欢运动加入计划”时，才显示运动习惯描述输入，界面语义更清晰、逻辑更一致。

---

## 4.3 AI 对话中心功能

### 4.3.1 多模式对话入口
输入框上方提供四种 AI 模式按钮，并高亮当前模式：
- 食物分析
- 制定计划
- 修改计划
- 其他（默认）

### 4.3.2 多模态输入体验
支持：
- 文本输入
- 图片选择上传
- 拖拽图片上传
- 粘贴图片上传

并提供上传预览与移除操作，交互闭环完整。

### 4.3.3 图片压缩策略
发送前进行前端压缩，兼顾识别质量与传输效率，降低延迟和后端存储压力。

### 4.3.4 流式步骤反馈
对话时可实时看到 AI 执行步骤名称（而非仅显示“思考中”），让用户理解当前进度。

### 4.3.5 结果结构化渲染
针对不同回答类型自动切换渲染方式：
- 食物分析结果：营养表格化
- 计划结果：训练表格化
- 其他问答：专业文本回答

### 4.3.6 会话管理
支持完整会话生命周期：
- 新建对话
- 首条消息自动命名
- 最近会话排序
- 历史会话切换
- 删除会话（确认弹窗）

并可自动加载更多历史消息，进入会话后默认定位到最新内容。

### 4.3.7 计划替换保护
当用户已有当前计划且再次发起“制定计划”时，系统会给出替换确认和倒计时机制，避免误覆盖。

---

## 4.4 AI Agent 功能（五大能力）

> 说明：此处将原 pipeline 统一表述为“AI Agent 功能”。

### 4.4.1 AI Agent：食物分析
- 支持文本食物名与图片识别。
- 结合食物数据库数据进行营养估算。
- 当外部候选不可靠时，具备模型兜底估算能力。
- 输出固定结构的每 100g 营养值（热量、蛋白质、脂肪、碳水）。

### 4.4.2 AI Agent：制定计划
- 生成“按月-按周-按天”的完整训练计划。
- 每天是可执行动作清单，不是笼统描述。
- 支持 `sets_reps` 字符串表达（兼容力量/有氧混合场景）。
- 每天独立整体强度、预计时长、组间间歇。
- 计划时长可动态匹配用户输入（默认 3 个月）。
- 对纯力量训练日施加强约束（动作数量与肌群覆盖），提高方案合理性。
- 月度目标避免模板化，强调与用户目标一致的阶段推进。

### 4.4.3 AI Agent：修改计划
- 仅按用户要求做定向修改，不重做整套计划。
- 输出包含变更摘要，用户可直观看到改动点。
- 支持禁忌动作识别与替换（如核心敏感动作替换为更安全方案）。
- 修改后继续满足训练结构与质量约束。

### 4.4.4 AI Agent：其他咨询
- 面向健康训练问题给出专业化、可执行建议。
- 强调动作标准、常见错误、纠正方法、风险规避、替代动作。
- 避免空泛回答，输出内容可直接落地实践。

### 4.4.5 AI Agent：记忆管理
- 在非食物模式对话后，后台异步整理用户长期记忆。
- 自动识别应沉淀信息（限制、伤病、目标、偏好、时间安排、器械条件等）。
- 去重、冲突处理（新信息优先），形成最终记忆集合。
- 为后续 AI 回答提供连续个性化上下文。

---

## 4.5 训练计划管理与执行闭环

### 4.5.1 当前计划与历史计划
系统支持当前计划展示，并保留有限历史版本，支持：
- 查看历史计划
- 删除历史计划
- 用历史计划替换当前计划

形成“可回退、可比较、可恢复”的计划管理能力。

### 4.5.2 今日计划
系统基于当前日期与计划周期自动计算“今日任务”，并展示：
- 今日动作清单
- 今日整体强度
- 今日预计时长
- 组间间歇提示

### 4.5.3 任务打卡与进度
支持逐项任务完成打卡，并自动计算完成进度条，增强执行反馈与坚持感。

---

## 4.6 身体数据与体重智能功能

### 4.6.1 身体数据总览
提供身体数据看板：BMI、体重、身高、年龄、核心目标、训练强度等，支持快速进入编辑与更新流程。

### 4.6.2 体重更新入口统一
在多个高频场景都可直接触发体重更新（身体数据页、今日计划页、资料页体重项），降低记录门槛。

### 4.6.3 体重记录状态化
体重记录支持“空腹/正常/饱腹”状态，保证数据可解释性更强。

### 4.6.4 体重智能换算与加权
系统以空腹为基准处理体重，并结合近期记录进行智能显示，核心特征包括：
- 同日多次记录聚合
- 异常值过滤
- 近期更高权重
- 参数随用户数据逐步个性化更新

### 4.6.5 体重趋势可视化
支持多时间粒度（日/三日/周）查看趋势，包含：
- 原始点
- 平滑趋势线
- 参考虚线
- 可滚动时间轴
- 悬浮数据提示

### 4.6.6 历史记录管理
支持查看历史体重记录并删除，删除后自动刷新并重算显示体重，保持数据一致。

---

## 4.7 个性化记忆可控功能

用户可在资料弹窗查看系统已沉淀记忆，并可进行删除（不允许直接编辑），保证：
- 个性化能力可见
- 用户对记忆有控制权
- 记忆体系可持续清理与维护

---

## 4.8 工程运行与可维护性功能

### 4.8.1 一键启动
通过根目录脚本可快速拉起 AI 服务、后端服务、前端服务，适合开发调试与演示。

### 4.8.2 全链路日志与错误点标记
系统对关键流程提供细粒度日志与错误点标识，有助于定位问题和稳定迭代。

### 4.8.3 长耗时任务兼容
对复杂 AI 任务提供较长超时窗口与流式反馈，提升稳定性与可用性。

---

## 五、项目价值总结

该项目已经从“单轮问答工具”演进为“面向长期健康管理的 AI 智能体系统”，其价值主要体现在：
- **可执行**：输出结构化、可直接落地到训练与饮食行为。
- **可持续**：有计划历史、聊天历史、体重趋势、记忆沉淀，支持长期使用。
- **可个性化**：基于用户画像 + 记忆 + 当前计划进行持续适配。
- **可闭环**：从建议生成到执行追踪再到动态调整，形成完整健康管理链路。

总体上，这是一套以“AI 智能体能力 + 健康场景产品化”深度结合为特征的项目，已经具备较完整的功能体系与持续迭代基础。
